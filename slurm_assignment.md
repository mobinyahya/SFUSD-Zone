ok i want to change the way that the benchmark config handles when matching is enabled. instead of writing each policy assignment and metrics into the zone subfolder, you should write all the assignments into the root folder. WHen export-aggregate-metrics is true in the assignment base config, you should treat every zone as its own policy subconfig. so there should be number_policy_subconfigs * num_zone_runs policy subconfigs after we inject results. Then we should have one metrics csv with all of the zones bascially being policy subconfigs. export-local-metrics should also also still work. FOcus on a clean solution, this task is about setting the pipeline from the benchmark package calling the assignment config in the nicest way such that we dont have to duplicate code. Its important that in the end there is just one csv with all matching metrics result in execution.output_dur/aggregate_metrics/metrics_citywide.csv add support for export-local-metrics: true (though i think if done correctly this should not require much if any additional code). Finaly ensure that this works with the slurm scripts and both the benchmark and assignment slurm scripts work