I would like to change how benchmark handles matching and choice metrics. I want to just call the assignnment code in matching. This should be a clean dependency with little to no overhead in the benchmark side. You should not have to recompute any seperate choice metrics by yourself. Do not create a seperate folder for matching and your own matching configs. Currently the benchmark configs take a list of policy configs in matching.configs. Instead, take a parameter of one assignment base config, and for each subconfig inject in the zone information by adding policy: - generated-zones and overwrite the location for the loaded zones. Then run with these parameters in mind. Completely remove the choice metrics part of the benchmark config, that should be handled entirely by export-aggregate-metrics, and export-local-metrics in the base config. For now ignore the effects on iterative_choice and just create breaking changes. It is important that you focus on creating clean and simple ways of doing this. also ignore the effecents on any analysis scripts. In terms of compute compute_stage_assignments, this would also mean computing metrics if they were enabled 

For doing this with slurm, you should utilize the fact that you can queue 8 more jobs than you can run. So use these 8 jobs to queue slurm assignment jobs and metrics, just using the more limited capacity. Reuse as much code and functionality as possible from the assignment slurm batching. Ideally, you should just call the slurm batching in assignment with different parameters (ie configuring the number of assignment and metrics jobs it gets to use, which is now 8 in total). Again focus on a clean implementation and remove code that is longer used after these changes, since there is a lot of redundancy in the matching code of benchmark and assignment. Treat the matching and matching metrics in assignment as the source of truth.