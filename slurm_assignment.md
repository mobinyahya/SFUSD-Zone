Look through the current slurm pipeline for both assignment and benchmarking. Turns out that the total number of submitted jobs is 12 and each node has at most 40 cpus. So I want to now reorganize how the slurm batching works. 

For both assignment and benchmarking you will always submit at most 12 "slurm" jobs. So to manage the hundreds of "work jobs" that I want you to complete you will need to use multiprocessing. Specifically each "slurm" job should request up to 40 cpus (be mindful not to waste any. Ie if i am requesting optimization runs that need 9 workers, you should only requeest 36 cpus since you cant do anything with the extra cores) to manage its tasks. Because of these hard computation limits, a single allocation should also do multiple sets of jobs. Ie if there were 12 * 40 * 3 assignment jobs, each slurm allocation would get 40 cores, and would manage its process pool to complete 40*3 among its 40 processes. Be mindful that for optimization runs that jobs may require more cpus per job, unlike assignment which only ever uses one cpu. 

Set the max cpus per node and the job limits as constants. Ask clarifying questions as needed. Do not worry aobut the way that the non slurm runners handle things, and contnue to ignore the benchmark configs capacity, max worker and max_tasks per worker constraints.